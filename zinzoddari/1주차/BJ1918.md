```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayDeque;
import java.util.Deque;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();

        Deque<Character> stack = new ArrayDeque<>();

        final String input = br.readLine();

        for (char c : input.toCharArray()) {
            // A-Z 사이 문자는 그대로 출력한다.
            if (c >= 'A' && c <= 'Z') {
                sb.append(c);
                continue;
            }

            // 입력 값이 좌측 괄호면 넘어간다.
            if (c == '(') {
                stack.push(c);
                continue;
            }

            // 오른쪽 괄호가 나오면, 왼쪽 괄호 나오기 전까지 스택에서 꺼내 출력한다.
            if (c == ')') {
                while (!stack.isEmpty()) {
                    final char temp = stack.pop();

                    if (temp == '(') {
                        break;
                    }

                    sb.append(temp);
                }
                continue;
            }

            while (!stack.isEmpty()) {
                if (stack.peek() == '(') {
                    break;
                }

                if (precedence(c) <= precedence(stack.peek())) {
                    sb.append(stack.pop());
                } else {
                    break;
                }
            }

            stack.push(c);
        }

        // 스택에 남은 모든 값을 출력합니다.
        while (!stack.isEmpty()) {
            if (stack.peek() == '(' || stack.peek() == ')') {
                stack.pop();
                continue;
            }
            sb.append(stack.pop());
        }

        System.out.println(sb);
    }

    private static int precedence(char c) {
        if (c == '*' || c == '/') return 2;
        if (c == '+' || c == '-') return 1;
        return 0;
    }
}
```

GPT랑 함께 풀었는데, 아마 혼자 풀면 절대 못풀 것 같습니다..<br/>
보니까 생각보다 시간을 많이 잡아먹는 문제라고 하네요<br/>
처음부터 너무 강한 친구를 만나서 너무 힘들어졌어요 ㅋㅋㅋ
